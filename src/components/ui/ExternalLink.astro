---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  href: string
  className?: string
}

const { href, className = '', ...attrs } = Astro.props
---

<a
  href={href}
  class={`text-brand-primary-500 after:content-['_↗'] ${className}`}
  {...attrs}
>
  <slot />
</a>
