---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  href: string
  className?: string
}

const { href, className = '', ...attrs } = Astro.props
---

<a href={href} class={`'inline-flex after:content-['_↗'] ${className}`} {...attrs}>
  <slot />
</a>
