---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  href: string
  className?: string
}

const { href, className = '', ...attrs } = Astro.props
---

<a
  href={href}
  class={`cursor-pointer hover:underline hover:decoration-2 hover:decoration-brand-primary-500 ${className}`}
  {...attrs}
>
  <slot />
</a>
